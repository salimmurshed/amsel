import '../../data/model/api_response_models/training_response.dart';

class TrainingConstants {
  static double minSeconds = 300; // 5 minutes = 300 seconds
  static double maxSeconds = 900; // 15 minutes = 900 seconds
  static double durationSeconds = 600; // 10 minutes = 600 seconds
  static ListElement listElement = ListElement();
}